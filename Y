#!/usr/bin/env bash

BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

show_banner() {
cat << "EOF"

╔══════════════════════════════════════════════╗
║            SYSWATCH SECURITY MONITOR        ║
║      System Health + Threat Detection       ║
╚══════════════════════════════════════════════╝

EOF
}

case "$1" in

vitals)
bash "$BASE_DIR/modules/vitals.sh"
;;

security)
bash "$BASE_DIR/modules/security.sh"
;;

score)
bash "$BASE_DIR/modules/score.sh"
;;

network)
bash "$BASE_DIR/modules/network.sh"
;;

about)
show_banner
echo
echo "SYSWATCH"
echo "Open source system health + security monitor"
echo
echo "Author: Burhan Abdullah"
echo "Language: Bash"
echo
;;

help)
show_banner
echo "COMMANDS"
echo
echo "syswatch           Full dashboard"
echo "syswatch vitals    Hardware stats"
echo "syswatch security  Security checks"
echo "syswatch network   Network monitoring"
echo "syswatch score     Vulnerability score"
echo "syswatch about"
echo
;;

*)
show_banner
bash "$BASE_DIR/modules/vitals.sh"
bash "$BASE_DIR/modules/security.sh"
bash "$BASE_DIR/modules/score.sh"
;;


EOF
}

case "$1" in

vitals)
bash "$BASE_DIR/modules/vitals.sh"
;;

security)
bash "$BASE_DIR/modules/security.sh"
;;

score)
bash "$BASE_DIR/modules/score.sh"
;;

network)
bash "$BASE_DIR/modules/network.sh"
;;

about)
show_banner
echo
echo "SYSWATCH"
echo "Open source system health + security monitor"
echo
echo "Author: Burhan Abdullah"
echo "Language: Bash"
echo
;;

help)
show_banner
echo "COMMANDS"
echo
echo "syswatch           Full dashboard"
echo "syswatch vitals    Hardware stats"
echo "syswatch security  Security checks"
echo "syswatch network   Network monitoring"
echo "syswatch score     Vulnerability score"
echo "syswatch about"
echo
;;

*)
show_banner
bash "$BASE_DIR/modules/vitals.sh"
bash "$BASE_DIR/modules/security.sh"
bash "$BASE_DIR/modules/score.sh"
;;

esac
